---
kind: example-tasks
feature: 001-loyalty-ledger
version: 0.1.0
status: example
description: Atomic TASK-NNN list for the loyalty-ledger spec example.
---

# Tasks — `loyalty-ledger` (example)

> Generated by `/sdd-tasks` from [`plan.md`](plan.md).
> Each task is atomic (≤1 day, 1-3 commits), references its REQ-NNN
> source, and has Given/When/Then acceptance criteria.

## TASK-001 — Scaffold the 8-module service

- **Status:** todo
- **References:** REQ-003 (skeleton precondition), RULE-002, RULE-004
- **Depends on:** —
- **Modules touched:** all 8 (creation)
- **Skill:** `lg5-new-service`
- **Command:** `/scaffold-service loyalty-ledger com.example.loyalty`
- **Acceptance:**
  - **Given** an empty repo
  - **When** the scaffolder finishes
  - **Then** `make install-skip-test` returns 0 and produces 8 jars

## TASK-002 — Add Avro consumer of `OrderPaidEvent`

- **Status:** todo
- **References:** REQ-003, REQ-004, RULE-007, RULE-010, ADR-002
- **Depends on:** TASK-001
- **Modules touched:** `message-core`
- **Skill:** `lg5-kafka-avro`
- **Command:** `/add-kafka-listener loyalty-ledger order-paid OrderPaidAvroModel`
- **Acceptance:**
  - **Given** a sample `OrderPaidAvroModel` message on `order-paid`
  - **When** the listener consumes it
  - **Then** the application-service input port is invoked exactly once

## TASK-003 — Define domain model: `Balance` + `LedgerEntry`

- **Status:** todo
- **References:** REQ-003, REQ-004, RULE-003, RULE-005, RULE-016
- **Depends on:** TASK-001
- **Modules touched:** `domain-core`
- **Skill:** `lg5-spring-overview`
- **Acceptance:**
  - **Given** unit tests for credit and the idempotency invariant
  - **When** they run
  - **Then** they pass with no Spring on the classpath of `domain-core`

## TASK-004 — Add JPA persistence + outbox table

- **Status:** todo
- **References:** REQ-001, REQ-002, REQ-003, RULE-008
- **Depends on:** TASK-003
- **Modules touched:** `data-access`
- **Skill:** `lg5-outbox`
- **Acceptance:**
  - **Given** Flyway migration `V1__init.sql`
  - **When** `make docker-up` and the IT suite runs
  - **Then** `balance`, `ledger_entry`, and `outbox` tables exist with the
    expected schema, and a balance-repo IT passes

## TASK-005 — Add outbox + scheduler for `points-credited`

- **Status:** todo
- **References:** REQ-005, RULE-008, RULE-011, RULE-014
- **Depends on:** TASK-003, TASK-004
- **Modules touched:** `application-service`, `data-access`
- **Skill:** `lg5-outbox`
- **Command:** `/add-outbox loyalty-ledger PointsCredited`
- **Acceptance:**
  - **Given** a credit invocation
  - **When** the credit's `@Transactional` boundary commits
  - **Then** an `outbox` row with `status=STARTED` exists in the same Tx,
    and the scheduler subsequently flips it to `COMPLETED` after publish

## TASK-006 — Wire the credit use case in the listener

- **Status:** todo
- **References:** REQ-003, REQ-004, RULE-008, RULE-009
- **Depends on:** TASK-002, TASK-003, TASK-005
- **Modules touched:** `application-service`
- **Skill:** `lg5-saga` (idempotency-guard section)
- **Acceptance:**
  - **Given** two deliveries of the same `OrderPaidEvent`
  - **When** both are consumed
  - **Then** only one ledger entry and one outbox row exist for that
    `(orderId)` (idempotency)

## TASK-007 — Add REST balance + ledger endpoints

- **Status:** todo
- **References:** REQ-001, REQ-002, RULE-005, RULE-006
- **Depends on:** TASK-001, TASK-004
- **Modules touched:** `api`
- **Skill:** `lg5-spring-overview`
- **Acceptance:**
  - **Given** a customer with a known balance
  - **When** `GET /loyalty/balances/{customerId}` is called with
    `Accept: application/vnd.api.v1+json`
  - **Then** the response is `200` with the value, or `404` if unknown

## TASK-008 — ATDD scenarios for credit, double-credit, not-found

- **Status:** todo
- **References:** REQ-001..REQ-005 (feature-level AC), RULE-012, RULE-013
- **Depends on:** TASK-006, TASK-007
- **Modules touched:** `acceptance-test`
- **Skill:** `lg5-atdd`
- **Subagent:** `lg5-test-generator`
- **Acceptance:**
  - **Given** the local infra (`docker-up`) is running
  - **When** `make run-acceptance-test` runs
  - **Then** the 3 named scenarios are green

## TASK-009 — Code review pass

- **Status:** todo
- **References:** all REQs, full constitution
- **Depends on:** TASK-008
- **Modules touched:** all
- **Subagent:** `lg5-code-reviewer`
- **Acceptance:**
  - **Given** the diff between TASK-001 and TASK-008
  - **When** `lg5-code-reviewer` runs
  - **Then** zero `must`-severity findings remain

## Definition of Done (Tasks)

- [ ] Every TASK references ≥1 REQ-NNN.
- [ ] Every TASK has Given/When/Then acceptance criteria.
- [ ] Every TASK is ≤1 day of work / 1-3 commits.
- [ ] Dependencies form a DAG (no cycles) — see plan.md.
- [ ] First TASK is the project skeleton; last TASK is "all ATDD green
      + zero must violations".
- [ ] Each TASK names the exact module(s), skill(s), and command(s)/
      subagent(s) it uses.
